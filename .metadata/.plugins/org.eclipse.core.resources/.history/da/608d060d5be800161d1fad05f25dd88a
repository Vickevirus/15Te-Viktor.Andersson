import java.util.Random;
import java.util.Scanner;

public class Anagram {

	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		Random r = new Random();

		StringBuilder remainder = new StringBuilder(sc.nextLine());

		while (remainder.length() > 0) {
			int index = r.nextInt(remainder.length()); // Random plats till bokstäver
			System.out.print(remainder.charAt(index)); // skriver in
			remainder.deleteCharAt(index); // Flyttar bokstaven från den ursprungliga platsen
		}

		System.out.println();

		sc.close();
	}
}